import { SafeAreaView, Text, View } from "react-native";

const HomePage = () => {
  return (
    <SafeAreaView>
      <View>
        <Text className="">Hello Native</Text>
      </View>
    </SafeAreaView>
  );
};

export default HomePage;
